# Architecture Decisions Log

Format: [ADR-00N] Title — Date — Status

- [ADR-001] Constitution-as-code at the repo root, enforced by git hooks + CI — adopted
- [ADR-002] Three enforcement layers: local hooks, server-side CI, self-audit (Law 16) — adopted
- [ADR-003] Canonical branches are permanent; deletion requires human approval (Law 15) — adopted
- [ADR-004] Cross-machine merges rehearsed on `merge/<topic>` staging first (Law 15.3) — adopted
- [ADR-005] Guard scripts have dedicated tests (Law 16.5); regression caught when the law loader silently fell back to a hardcoded list — adopted
- [ADR-006] Internal reasoning toggle: `show_internal_reasoning` + `reasoning_log` in `.guardrail.json`, surfaced via `lawkeeper reasoning` and `lawkeeper run --show-reasoning/--hide-reasoning`; default hidden — adopted
- [ADR-007] Executor layer: framework-agnostic `Task`/`Executor`/`ExecutorResult` protocol; backends (subprocess now, OpenAI-compatible planned) behind one interface; explicit `ExecutorResult`→`CheckResult` adapter; wired via `lawkeeper run --executor`. Context: agentic + vibe-coded tasks need a governed execution path decoupled from any single agent. Consequence: callers never depend on a backend; new backends plug in without touching core. Contract: `docs/EXECUTOR_CONTRACT.md`. Originally proposed by the laptop as ADR-006 (2026-08-07); renumbered here since ADR-006 was independently already taken by the reasoning-toggle decision above by the time this merged — real number collision, not duplicate content. Ratified 2026-08-20: only `SubprocessExecutor` is actually implemented so far (`src/guardrail/executor.py`); the OpenAI-compatible backend and the `ExecutorResult`→`CheckResult` adapter itself remain PLANNED, not built — adopted (interface), PLANNED (OpenAI backend + adapter)
- [ADR-008] Removed `src/guardrail/agent.py` (Stage 1 of `docs/LAWKEEPER_PROTOTYPE_PLAN.md`'s Agent SDK integration); kept `src/guardrail/executor.py` (ADR-007). Context: `docs/ARCHITECTURE_REVIEW_2026-09-04.md` found both modules fully built and tested but with zero real callers anywhere outside their own test files, weeks after being built — both shipped as real surface area in every `pip install lawkeeper`, and `agent.py` additionally declared an unused optional `claude-agent-sdk` dependency. The review deliberately did not decide between them (Law 21: "not something to decide unilaterally mid-review"); asked the user directly. Decision (2026-09-04): remove `agent.py` (Stage 1 had stalled longest, no Stage 2-4 progress), keep `executor.py` (ratified via ADR-007, its interface is closer to being wired in, only the OpenAI-compatible backend remains PLANNED). Consequence: `tests/test_agent.py`, `test_governance/cards/test_agent.yaml`, and the `agent` optional-dependency extra removed with it; `docs/LAWKEEPER_PROTOTYPE_PLAN.md` kept as historical record with a status note, not deleted — recoverable from git history if Stage 2+ resumes — adopted (removed)
- [ADR-009] Split `lawkeeper init`'s shipped template: `ai_review.py`, `consensus_review.py`, `team_chat.py` (general multi-model/agent-coordination tools, not governance enforcement) moved out of the always-installed `src/guardrail/template/` into an opt-in `src/guardrail/template_extras/`, installed only with `lawkeeper init --with-tools`. Context: `docs/ARCHITECTURE_REVIEW_2026-09-04.md` found the default scaffold's scope didn't match the package's stated purpose ("constitution-as-code governance") — those 3 tools check or enforce nothing, while genuinely governance-related dev tools (`mine_failure_patterns.py`, `code_compliance_checker.py`, `adversarial_review_checker.py`) were deliberately kept unshipped. Decision (2026-09-04, user's direct call per the review's own recommendation): keep both, but as two honestly-named things instead of one blurred default. Consequence: a fresh `lawkeeper init` ships governance-only; `--with-tools` adds the LLM-orchestration bundle for projects that want it — adopted
- [ADR-010] Ported `import-linter` from Windwright to mechanically enforce `src/guardrail/`'s own internal layering (`.importlinter` at the repo root; wired into `scripts/system_audit.py`'s `check_import_boundaries()`, which CI already runs). Context: real user directive 2026-09-04 (`shared_memory/user-quality-standard-escalation-and-cross-repo-sharing-2026-09-04.md`) to check tools found in one repo for portability to others before building something new. Of the 3 candidates that directive named, only this one had a real, existing target in lawkeeper: `docs/ARCHITECTURE_REVIEW_2026-09-04.md`'s manually-grepped claim ("core/, laws/, and memory/ never import cli.py or config.py") is now a mechanically-checked contract instead — and writing it exposed the original claim was itself slightly wrong (`core/runner.py` has a real, legitimate deferred import of `config.py`; only `cli.py` needed to stay forbidden). Windwright's other two candidates (`check_tool_registry.py`, `validate_locked_constants.py`) were deliberately NOT ported: this repo has no `docs/TOOL_REGISTRY.md` and no `# LOCKED:` spans anywhere for either to check against — porting either now would itself be the exact anti-pattern the directive names ("tools built and not wired in is way worse than no tools built"). Consequence: `pyproject.toml`'s `dev` extra gained `import-linter`; a real, verified adversarial test (`tests/test_system_audit.py`) proves the check actually catches a broken contract, not just reports clean — adopted

(Add your own decisions here; one short paragraph each: context, decision, consequence.)

## ADR-006 context/decision/consequence
A model's internal reasoning (chain-of-thought) is useful for audit and cross-
machine handoff but noisy for clean project artifacts. Decision: keep the toggle
off by default (no reasoning written unless opted in per repo), store the flag in
`.guardrail.json` so it is shared across desktop/laptop, and expose two CLI
surfaces: `lawkeeper reasoning [TEXT]` to record/query the toggle, and
`run --show-reasoning/--hide-reasoning` to override it for a single check.
Consequence: reasoning capture is explicit, version-controlled, and never leaks
into a repo that hasn't opted in.
